<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Flower Power</title>

    <!-- Bootstrap core CSS -->
    <link href="~/Content/Shop/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="~/Content/Shop/css/shop-homepage.css" rel="stylesheet">


    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <img src="~/Content/Images/Filialen/logoFlowerPower.jpg" style="max-width:100px"/>
            <!-- TODO: Logo van flowerpower HIER -->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="@Url.Action("Index", "Home")"><span>Home</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Bestellen", "Bloemen")"><span>Webshop</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Contact")"><span>Contact</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("FacturenLijst", "FactuurRegel")"><span>Facturen</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Edit", "Klant")"><span>Profiel wijzigen</span></a>
                    </li>

                    @if (this.User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "BloemenCRUD", new { Area = "Admin"})"><span>CMS</span></a>
                        </li>
                    }
                    @if (this.User.IsInRole("Medewerker"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "BloemenCRUD", new { Area = "Admin"})"><span>CMS</span></a>
                        </li>
                    }
                    <li class="nav-item">
                        @Html.Partial("_LoginPartial")
                    </li>
                </ul>
            </div>
        </div>
    </nav>


    @*<div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("FlowerPower", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("Webshop", "Index", "Bloemen")</li>
                    <li>@Html.ActionLink("Mijn Facturen", "Index", "Controllername")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                    @if (ViewContext.HttpContext.User.IsInRole("Admin") || ViewContext.HttpContext.User.IsInRole("Medewerker1") || ViewContext.HttpContext.User.IsInRole("Medewerker2"))
                    {
                        <li>@Html.ActionLink("CMS", "Index", "BloemenCRUD", new { Area = "Admin" }, new { })</li>
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>*@
    <div class="container">
        <div class="row">
            @RenderBody()
        </div>
       
    </div>

        <!-- Footer -->
        <footer class="py-5 bg-dark">
            <div class="container">
                <p class="m-0 text-center text-white">Deze machtig mooie website wordt mede mogelijk gemaakt en is ontwikkeld door Christianwanschers.nl. Copyright &copy; FlowerPower.nl 2018</p>
            </div>
            <!-- /.container -->
        </footer>

    <!-- Bootstrap core JavaScript -->
    <script src="~/Content/Shop/vendor/jquery/jquery.min.js"></script>
    <script src="~/Content/Shop/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
